import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gabrielalonso.typescriptpad',
  appName: 'TypeScript Pad',
  webDir: 'dist',
  android: { backgroundColor: '#0d1117' },
};

export default config;
